{{- if and .Values.logchef.enabled .Values.logchef.httproute.create .Values.gateway.name (include "logchef.publicDomain" .) }}
{{- $gateway := .Values.gateway.name }}
{{- $gatewayNamespace := .Values.gateway.namespace }}
{{- $domain := include "logchef.publicDomain" . }}
{{- $host := printf "%s.%s" (include "logchef.logchefServiceName" .) $domain }}
{{- $svc := include "logchef.logchefServiceName" . }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ $svc }}-redirect
  labels:
    {{- include "logchef.labels" . | nindent 4 }}
    app.kubernetes.io/component: logchef
  {{- with .Values.commonAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  hostnames:
    - {{ $host | quote }}
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: {{ $gateway }}
      {{- if $gatewayNamespace }}
      namespace: {{ $gatewayNamespace }}
      {{- end }}
      sectionName: {{ .Values.gateway.httpSectionName | default "http" }}
  rules:
    - filters:
        - type: RequestRedirect
          requestRedirect:
            scheme: https
            port: 443
            statusCode: 301
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ $svc }}
  labels:
    {{- include "logchef.labels" . | nindent 4 }}
    app.kubernetes.io/component: logchef
  {{- with .Values.commonAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  hostnames:
    - {{ $host | quote }}
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: {{ $gateway }}
      {{- if $gatewayNamespace }}
      namespace: {{ $gatewayNamespace }}
      {{- end }}
      sectionName: {{ .Values.gateway.httpsSectionName | default "https" }}
  rules:
    - backendRefs:
        - group: ""
          kind: Service
          name: {{ $svc }}
          port: {{ .Values.logchef.service.port }}
          weight: 1
      matches:
        - path:
            type: PathPrefix
            value: {{ .Values.logchef.httproute.path | default "/" | quote }}
{{- end }}
