{{- if .Values.logchef.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "logchef.logchefServiceName" . }}
  labels:
    {{- include "logchef.labels" . | nindent 4 }}
    app.kubernetes.io/component: logchef
  {{- with .Values.commonAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.logchef.service.type }}
  ports:
    - name: http
      port: {{ .Values.logchef.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "logchef.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: logchef
{{- end }}
